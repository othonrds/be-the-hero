import React from 'react';
import ReactDOM from 'react-dom'; //dom =  ávore de elementos
import App from './App';


ReactDOM.render(
  <React.StrictMode>  
    <App />
  </React.StrictMode>, //??
  document.getElementById('root')
);


//render = renderização : "colocar em tela"